这个程序的功能是计算一个串中的所有子串中出现次数大于Ｎ次的所有符合条件的子串
（当然也可以对所有的串进行整合，这个可以在trie树的遍历上进行整合）
主要利用的算法是后缀数组和trie树的辅助
利用height数组得出相邻两个排序后的子串的公共前缀，最后插入的时候根据前面一个
得出的结果长度进行判断处理，如果大于那么记住上一次插入字典树的位置接着插入，
因为前面的部分是相同的，不需要重复插入字典树，如果小于，那么直接回退差值步
注意这个程序只是实现一个算法思想，对于只支持1234四个字符，如果想支持其他字符
可以自己改trie树的实现，这不是问题
